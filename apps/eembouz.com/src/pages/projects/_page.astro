---
import { render } from "astro:content";
import type { CollectionEntry } from "astro:content";
import Layout from "../../layouts/Layout.astro";
import MetaDescription from "../../components/MetaDescription.astro";
import Prose from "@portfolio/ui/Prose";
import { supportsViewTransitions } from "astro:transitions/client";

interface Props {
  project: CollectionEntry<"projects">;
}

const { project } = Astro.props;

const { Content } = await render(project);

const { seo: seoProps, title, thumbnail, summary, techno } = project.data;

const metaData = {
  title: project.data.title,
  description: project.data.description,
  image: project.data.thumbnail,
};

const seo = seoProps ? seoProps : metaData;
---

<Layout {...seo}>
  <div class="container">
    <section class="projects">
      <div class="meta w-[550px] h-full">
        <MetaDescription
          thumbnail={thumbnail}
          title={title}
          summary={summary}
          techno={techno}
        />
      </div>

      <div class="content">
        <div class="container">
          <Prose>
            <Content />
          </Prose>
        </div>
      </div>
    </section>
  </div>
</Layout>

<style>
  .projects {
    position: relative;
    display: flex;
    /* align-items: start; */
    flex-direction: column;
    gap: 2rem;
    z-index: 2;
    padding-block: 3rem;
  }

  @media screen and (min-width: 1200px) {
    .projects {
      position: relative;
      display: grid;
      /* align-items: start; */
      grid-template-columns: repeat(2, 1fr);
      z-index: 2;
      /* padding-top: 5rem; */
    }
  }

  @media screen and (max-width: 590px) {
    .container-narrow .info .tags {
      align-items: flex-start;
      flex-direction: column;
    }
  }

  .content {
    /* background: red; */
    padding-top: 30rem;
    strong {
      font-weight: 900;
    }

    h3,
    h4,
    h5,
    h6 {
      padding-top: 1.5rem;
      letter-spacing: normal;
      font-family: "Inter" !important;
      color: var(--color-primary);
      font-weight: 700;
    }

    h2 {
      text-align: left;
      white-space: normal;
      word-break: break-word;
      line-height: 1.4;
      font-weight: 100;
      font-size: 2rem;
      font-style: italic;
    }

    img {
      border-radius: 0.5rem;
    }

    video {
      border-radius: 0.5rem;
      width: -webkit-fill-available;
    }
  }
</style>
